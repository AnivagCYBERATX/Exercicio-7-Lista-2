function substituirElemento(arr, valorAntigo, valorNovo) {
    return arr.map(num => num === valorAntigo ? valorNovo : num);
}

// Exemplo de uso:
console.log(substituirElemento([1, 2, 3, 2, 4], 2, 5));
// Saída: [1, 5, 3, 5, 4]
